/* Al presionar el Botón, asignar una nota RANDOM al examen y mostrar:
"EXCELENTE" para notas igual a 9 o 10,
"APROBÓ" para notas mayores a 4,
"Vamos, la proxima se puede" para notas menores a 4 function mostrar() */


{
	var numRandom;
	var numRandom=(Math.floor((Math.random() * 10) +1));

	/*if(numRandom < 1)
	{
		alert("El numero random es"+numRandom+"Vamos, la proxima se puede.");
	} */
	

	/*else {
		if(numRandom > 8 )
        {
	    alert("El numero random es"+numRandom+"EXCELENTE.");
	    }  
         else
	    {
		alert("Vamos, la proxima se puede");
		}
	} */
	
 	
}
